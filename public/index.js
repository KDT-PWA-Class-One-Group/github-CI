window.addEventListener("DOMContentLoaded", function () {
  // 모든 DOM이 로드된 후 실행, dom === HTML, CSS

  console.log("hello! github ci!");

  const root = document.getElementById("root");
  console.log('root :', root);


});
